# Copy to .env (gitignored). Required before any crawl of terra.snellman.net
# or tmtour.org: every request carries this address in its User-Agent so the
# site operators can reach whoever is running the crawler.
BGAI_CRAWL_CONTACT=you@example.org
